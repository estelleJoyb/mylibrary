{% extends 'base.html.twig' %}

{% block title %}
	{{ parent() }}
	|
												Modification de la catégorie
	{{ categorie.titre }}
{% endblock %}
{% block body %}
	<section class="container mt-4">
		<h1 class="text-center">Modification de la catégorie
			{{ categorie.titre }}</h1>
		{% include 'Frontend/categorie/_form.html.twig' with {button_label: 'Modifier'} %}
		<a href="{{ path('app.categorie.index') }}" class="btn btn-secondary mt-4">Retour à la liste</a>
	</section>
{% endblock %}
